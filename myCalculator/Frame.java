package myCalculator;

import javax.swing.JFrame;

public class Frame extends JFrame {
    public Frame(String title){
        setTitle(title);
        setSize(500, 500);  //tried to set size half of the screenDimension. But it doesn't support. Don't know why.
        

    }
}
